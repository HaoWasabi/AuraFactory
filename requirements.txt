# AuraFactory — Production Dependencies
# =======================================

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Discord bot
nextcord>=2.6.0

# Database (PostgreSQL — fallback, optional)
# asyncpg>=0.29.0

# LLM providers (Gemini — fallback for local dev with free tier)
google-generativeai>=0.8.0  # Optional: only needed if LLM_PROVIDER=gemini

# AWS Bedrock (Phase 2)
boto3>=1.34.0
aws-xray-sdk>=2.13.0

# Ollama (local/self-hosted LLM)
httpx>=0.27.0

# HTTP client (OAuth2, Discord API)
aiohttp>=3.9.0

# Config & utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0.0

# Graph (tool dependency graph)
networkx>=3.0

# Security
python-multipart>=0.0.6

# Templates (web dashboard)
jinja2>=3.1.0

# Observability (fallback — AWS uses CloudWatch EMF natively)
prometheus-client>=0.20.0
