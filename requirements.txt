# ============================================================
# AuraFactory — Requirements
# ============================================================

# === CORE: Discord Bot ===
nextcord>=2.6.0
python-dotenv>=1.0.0

# === PHASE 1: LLM Provider (Gemini — free/open) ===
google-generativeai>=0.8.0

# === API SERVER (Frontend ↔ Agent System) ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# === PHASE 2: AWS (uncomment khi tích hợp) ===
# boto3>=1.34.0              # AWS SDK
# botocore>=1.34.0           # AWS core

# === AGENT FRAMEWORK (nếu dùng Strands sau) ===
# strands-agents>=0.1.0      # Phase 2: AWS open-source agent framework

# === UTILITIES ===
aiohttp>=3.9.0               # Async HTTP (Discord API backup)
asyncio-extras>=1.3.0        # Async utilities (nếu cần)

# === OBSERVABILITY ===
# Phase 1: Dùng built-in logging + json files
# Phase 2: uncomment below
# aws-xray-sdk>=2.12.0       # AWS X-Ray tracing
# opentelemetry-api>=1.20.0  # OpenTelemetry (nếu dùng)

# === KNOWLEDGE BASE (Phase 1: local) ===
# chromadb>=0.4.0            # Local vector DB (nếu cần RAG local)
# sentence-transformers>=2.2.0  # Embedding model local

# === DEV/TEST ===
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
