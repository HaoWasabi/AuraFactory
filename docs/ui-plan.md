Giao diện được thiết kế theo phong cách tối giản, tập trung vào không gian trò chuyện và tối ưu hóa trải nghiệm tương tác đa phương thức.

Dưới đây là các thành phần giao diện chính:

### 1. Khung nhập liệu (Input Chatbox)

Đây là nơi bạn bắt đầu mọi cuộc hội thoại, nằm ở cạnh dưới cùng của màn hình. Nó tích hợp nhiều công cụ đa phương thức:

* **Hộp văn bản:** Nơi nhập câu lệnh (prompt).
* **Nút tải tệp/hình ảnh lên (Upload icon):** Cho phép bạn tải lên hình ảnh, tài liệu (PDF, Word, Excel...).
* **Nút gửi (Send):** Biểu tượng mũi tên hoặc máy bay giấy để gửi yêu cầu.

### 2. Khu vực hiển thị cuộc trò chuyện (Chat Feed)

Chiếm phần lớn diện tích màn hình ở trung tâm, hiển thị lịch sử tương tác giữa bạn và AI.

* **Nội dung phản hồi:** Văn bản được định dạng rõ ràng (Markdown), hỗ trợ hiển thị các khối code (Code block) có tính năng sao chép nhanh, bảng biểu và sơ đồ.
* **Thanh công cụ dưới mỗi phản hồi:** * *Chấp thuận (Agree):* Để phản hồi người dùng phản hồi đồng ý với kế hoạch AI dự kiến triển khai.
* *Chia sẻ & Xuất (Share & Export):* Xuất nhanh câu trả lời sang file markdown.
* *Sao chép (Copy):* Copy nhanh toàn bộ văn bản phản hồi.
* *Xem các bản nháp khác (View other drafts):* Cho phép bạn xem các phương án trả lời khác mà AI đã tạo ra cho cùng một câu lệnh.



### 3. Thanh menu bên (Sidebar)

Nằm ở phía bên trái màn hình (có thể ẩn/hiện bằng biểu tượng 3 dấu gạch ngang ở góc trên bên trái) để quản lý không gian làm việc:

* **Cuộc trò chuyện mới (New chat):** Nút bấm để mở một cửa sổ chat hoàn toàn sạch, không bị ảnh hưởng bởi ngữ cảnh cũ.
* **Lịch sử trò chuyện (Recent/History):** Danh sách các cuộc hội thoại gần đây, cho phép bạn đổi tên, ghim (Pin) các đoạn chat quan trọng hoặc xóa chúng.
* **Góc dưới Sidebar:** Thường chứa các tùy chọn Cài đặt (Settings - đổi giao diện Sáng/Tối), Trợ giúp (Help) và Hoạt động Gemini (Gemini Activity).

### 4. Thanh điều hướng và Tiện ích mở rộng (Header & Extensions)

Nằm ở bên phải góc trên cùng của giao diện:

* **Menu Tài khoản:** Góc trên cùng bên phải hiển thị ảnh đại diện tài khoản Discord của bạn, nơi quản lý tài khoản và chuyển đổi giữa các không gian làm việc, đăng xuất. 
  
---
Giao diện đa nền tảng, hiển thị tối ưu cho điện thoại và máy tính.
Màu sắc & Font chữ lấy cảm hứng từ Mee6 Dashboard. Link: https://mee6.xyz/en/dashboard/
Bố cục & Luồng trải nghiệm lấy cảm hứng từ Gemini.
